#include "main.h"

/**
 * _strlen - Function that returns the lenght of a string.
 * @s: Type char pointer
 * Return: return (c)
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}

		return (c);

}
