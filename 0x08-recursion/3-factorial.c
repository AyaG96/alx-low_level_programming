#include "main.h"
/**
 * factorial - function that returns the factorial of a given number
 *
 * @n: a given number
 *
 * Return: 0, -1 or the result
 */

int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (1);
	}
	else if
	{
		return (n * factorial(n - 1));
	}
}
